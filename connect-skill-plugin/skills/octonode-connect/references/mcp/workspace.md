# MCP operations: workspace

Generated by `yarn gen:mcp-skills`. Do not edit this file manually.

Read the [shared calling contract](../mcp-calling.md). [All operations](index.md).

Examples use placeholder IDs/content and zero revisions. Replace them with live values before calling.

- [platform_capabilities](#platform-capabilities)
- [profile_get](#profile-get)
- [profile_avatar_get](#profile-avatar-get)
- [workspace_members](#workspace-members)
- [conversations](#conversations)
- [conversation_members](#conversation-members)
- [messages](#messages)
- [notifications](#notifications)

## platform-capabilities

Tool name: `platform_capabilities`

Read the server-declared platform mode and feature availability.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/capabilities`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

### Example arguments

```json
{}
```

## profile-get

Tool name: `profile_get`

Get the caller's workspace profile.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/profile`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

### Example arguments

```json
{}
```

## profile-avatar-get

Tool name: `profile_avatar_get`

Read a workspace member avatar as base64.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/profiles/{userId}/avatar`.
- Query arguments: `v`.
- JSON body arguments: none.
- Result: data.contentType and data.base64.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "userId": {
      "type": "string"
    },
    "v": {
      "type": "integer"
    }
  },
  "required": [
    "userId"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "userId": "example-id"
}
```

## workspace-members

Tool name: `workspace_members`

Search workspace member profiles.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/profiles/workspace-members`.
- Query arguments: `q`, `cursor`, `limit`.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "q": {
      "type": "string"
    },
    "cursor": {
      "type": "string"
    },
    "limit": {
      "type": "integer"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

### Example arguments

```json
{}
```

## conversations

Tool name: `conversations`

List workspace conversations.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/social/conversations`.
- Query arguments: `cursor`, `limit`.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "cursor": {
      "type": "string"
    },
    "limit": {
      "type": "integer"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

### Example arguments

```json
{}
```

## conversation-members

Tool name: `conversation_members`

List a conversation's member ids.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/social/conversations/{id}/members`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "id": "example-id"
}
```

## messages

Tool name: `messages`

List paginated conversation messages.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/social/conversations/{conversationId}/messages`.
- Query arguments: `cursor`, `limit`.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "conversationId": {
      "type": "string"
    },
    "cursor": {
      "type": "string"
    },
    "limit": {
      "type": "integer"
    }
  },
  "required": [
    "conversationId"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "conversationId": "example-id"
}
```

## notifications

Tool name: `notifications`

List notifications and unread count.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/social/notifications`.
- Query arguments: `cursor`, `limit`.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "cursor": {
      "type": "string"
    },
    "limit": {
      "type": "integer"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

### Example arguments

```json
{}
```
