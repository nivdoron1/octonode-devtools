# MCP operations: plugins

Generated by `yarn gen:mcp-skills`. Do not edit this file manually.

Read the [shared calling contract](../mcp-calling.md). [All operations](index.md).

Examples use placeholder IDs/content and zero revisions. Replace them with live values before calling.

- [project_plugins](#project-plugins)
- [project_plugin](#project-plugin)
- [project_plugin_node_add](#project-plugin-node-add)

## project-plugins

Tool name: `project_plugins`

List installed plugins attached to the bound project.

- Effect: **read** (registration metadata).
- Scope: workspace and selected project; projectId overrides the connection default.
- HTTP: `GET /api/plugins?fields=id,name,version,description,icon,nodes`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    }
  },
  "required": [],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "projectId": "example-project"
}
```

## project-plugin

Tool name: `project_plugin`

Inspect an installed plugin's pinned version and up to 100 nodes, including custom library exports and original npm implementation identities when available.

- Effect: **read** (registration metadata).
- Scope: workspace and selected project; projectId overrides the connection default.
- HTTP: `GET /api/plugins/{pluginId}?limit=100`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    },
    "pluginId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    }
  },
  "required": [
    "pluginId"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "pluginId": "example-id",
  "projectId": "example-project"
}
```

## project-plugin-node-add

Tool name: `project_plugin_node_add`

Add one installed plugin node with a stable instance id.

- Effect: **write** (registration metadata).
- Scope: workspace and selected project; projectId overrides the connection default.
- HTTP: `POST /api/plugins/{pluginId}/nodes/{nodeId}/add`.
- Query arguments: none.
- JSON body arguments: `instanceId`, `workflowId`, `position`.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    },
    "pluginId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "nodeId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "instanceId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "workflowId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "position": {
      "type": "object",
      "properties": {
        "x": {
          "type": "number"
        },
        "y": {
          "type": "number"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "pluginId",
    "nodeId",
    "instanceId"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "pluginId": "example-id",
  "nodeId": "example-id",
  "instanceId": "example-id",
  "projectId": "example-project"
}
```
