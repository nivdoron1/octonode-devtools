# MCP operations: github

Generated by `yarn gen:mcp-skills`. Do not edit this file manually.

Read the [shared calling contract](../mcp-calling.md). [All operations](index.md).

Examples use placeholder IDs/content and zero revisions. Replace them with live values before calling.

- [github_repositories](#github-repositories)
- [pull_requests](#pull-requests)
- [pull_request](#pull-request)
- [workflow_diff](#workflow-diff)
- [review_file](#review-file)
- [review_threads](#review-threads)
- [github_job](#github-job)
- [project_pull_request_file](#project-pull-request-file)
- [project_pull_requests](#project-pull-requests)
- [project_pull_request](#project-pull-request)

## github-repositories

Tool name: `github_repositories`

List repositories visible to connected GitHub App installations.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/github/repositories`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

### Example arguments

```json
{}
```

## pull-requests

Tool name: `pull_requests`

List Architecture pull requests and the true default branch.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/architectures/{architectureId}/github/pull-requests`.
- Query arguments: `state`.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "architectureId": {
      "type": "string"
    },
    "state": {
      "type": "string",
      "enum": [
        "open",
        "closed",
        "all"
      ]
    }
  },
  "required": [
    "architectureId"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "architectureId": "example-id"
}
```

## pull-request

Tool name: `pull_request`

Get one immutable Architecture pull-request context and project impact.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/architectures/{architectureId}/github/pull-requests/{number}`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "architectureId": {
      "type": "string"
    },
    "number": {
      "type": "integer"
    }
  },
  "required": [
    "architectureId",
    "number"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "architectureId": "example-id",
  "number": 1
}
```

## workflow-diff

Tool name: `workflow_diff`

Project an Architecture PR diff onto one contained project.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/architectures/{architectureId}/github/pull-requests/{number}/workflow-diff`.
- Query arguments: `projectId`.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "architectureId": {
      "type": "string"
    },
    "number": {
      "type": "integer"
    },
    "projectId": {
      "type": "string"
    }
  },
  "required": [
    "architectureId",
    "projectId",
    "number"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "architectureId": "example-id",
  "projectId": "example-id",
  "number": 1
}
```

## review-file

Tool name: `review_file`

Read one bounded Architecture base/head file and its changed lines.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/architectures/{architectureId}/github/pull-requests/{number}/review-file`.
- Query arguments: `path`.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "architectureId": {
      "type": "string"
    },
    "number": {
      "type": "integer"
    },
    "path": {
      "type": "string"
    }
  },
  "required": [
    "architectureId",
    "number",
    "path"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "architectureId": "example-id",
  "number": 1,
  "path": "src/example.ts"
}
```

## review-threads

Tool name: `review_threads`

List review threads for one contained project.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/architectures/{architectureId}/github/pull-requests/{number}/review-threads`.
- Query arguments: `projectId`, `workflowId`, `cursor`, `limit`.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "architectureId": {
      "type": "string"
    },
    "number": {
      "type": "integer"
    },
    "projectId": {
      "type": "string"
    },
    "workflowId": {
      "type": "string"
    },
    "cursor": {
      "type": "string"
    },
    "limit": {
      "type": "integer"
    }
  },
  "required": [
    "architectureId",
    "projectId",
    "number"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "architectureId": "example-id",
  "projectId": "example-id",
  "number": 1
}
```

## github-job

Tool name: `github_job`

Read an asynchronous GitHub job.

- Effect: **read** (registration metadata).
- Scope: workspace; any projectId argument is forwarded only as declared below.
- HTTP: `GET /api/github/jobs/{id}`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "id": "example-id"
}
```

## project-pull-request-file

Tool name: `project_pull_request_file`

Read a PR file's base/head source and changed lines inside the authorized project. Check headSha against the PR before reviewing; never publish a review without user approval.

- Effect: **read** (registration metadata).
- Scope: workspace and selected project; projectId overrides the connection default.
- HTTP: `GET /api/architectures/{architectureId}/github/pull-requests/{number}/review-file`.
- Query arguments: `path`.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    },
    "architectureId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "number": {
      "type": "string",
      "pattern": "^[1-9][0-9]*$",
      "minLength": 1,
      "maxLength": 10
    },
    "path": {
      "type": "string",
      "minLength": 1,
      "maxLength": 4096
    }
  },
  "required": [
    "architectureId",
    "number",
    "path"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "architectureId": "example-id",
  "number": "1",
  "path": "src/example.ts",
  "projectId": "example-project"
}
```

## project-pull-requests

Tool name: `project_pull_requests`

List pull requests for the GitHub architecture containing the authorized project. Ask the user to connect GitHub if unavailable.

- Effect: **read** (registration metadata).
- Scope: workspace and selected project; projectId overrides the connection default.
- HTTP: `GET /api/architectures/{architectureId}/github/pull-requests`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    },
    "architectureId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    }
  },
  "required": [
    "architectureId"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "architectureId": "example-id",
  "projectId": "example-project"
}
```

## project-pull-request

Tool name: `project_pull_request`

Read a pull request and its immutable base/head references in the authorized project's GitHub architecture.

- Effect: **read** (registration metadata).
- Scope: workspace and selected project; projectId overrides the connection default.
- HTTP: `GET /api/architectures/{architectureId}/github/pull-requests/{number}`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    },
    "architectureId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "number": {
      "type": "string",
      "pattern": "^[1-9][0-9]*$",
      "minLength": 1,
      "maxLength": 10
    }
  },
  "required": [
    "architectureId",
    "number"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "architectureId": "example-id",
  "number": "1",
  "projectId": "example-project"
}
```
