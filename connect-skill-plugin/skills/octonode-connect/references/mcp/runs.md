# MCP operations: runs

Generated by `yarn gen:mcp-skills`. Do not edit this file manually.

Read the [shared calling contract](../mcp-calling.md). [All operations](index.md).

Examples use placeholder IDs/content and zero revisions. Replace them with live values before calling.

- [project_runs](#project-runs)
- [project_run](#project-run)
- [project_workflow_run](#project-workflow-run)
- [project_workflow_cancel](#project-workflow-cancel)

## project-runs

Tool name: `project_runs`

List the latest 50 bounded workflow run summaries.

- Effect: **read** (registration metadata).
- Scope: workspace and selected project; projectId overrides the connection default.
- HTTP: `GET /api/runs?limit=50&fields=runId,workflowId,status,durationMs,startedAt,nodeCount`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    }
  },
  "required": [],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "projectId": "example-project"
}
```

## project-run

Tool name: `project_run`

Read one persisted workflow run.

- Effect: **read** (registration metadata).
- Scope: workspace and selected project; projectId overrides the connection default.
- HTTP: `GET /api/runs/{runId}`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    },
    "runId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "runId": "example-id",
  "projectId": "example-project"
}
```

## project-workflow-run

Tool name: `project_workflow_run`

Run one workflow and return its bounded event stream.

- Effect: **write** (registration metadata).
- Scope: workspace and selected project; projectId overrides the connection default.
- HTTP: `POST /api/workflows/{workflowId}/run`.
- Query arguments: none.
- JSON body arguments: `env`, `input`, `fromNode`, `pins`.
- Result: bounded event stream normalized to data.events.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    },
    "workflowId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "env": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "input": {
      "type": "object",
      "maxProperties": 100
    },
    "fromNode": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "pins": {
      "type": "object",
      "maxProperties": 100
    }
  },
  "required": [
    "workflowId"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "workflowId": "example-id",
  "projectId": "example-project"
}
```

## project-workflow-cancel

Tool name: `project_workflow_cancel`

Idempotently cancel one active workflow run.

- Effect: **write; destructive** (registration metadata).
- Scope: workspace and selected project; projectId overrides the connection default.
- HTTP: `POST /api/runs/{runId}/cancel`.
- Query arguments: none.
- JSON body arguments: none.
- Result: upstream JSON in data.

### Input schema

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    },
    "runId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

### Example arguments

```json
{
  "runId": "example-id",
  "projectId": "example-project"
}
```
