# project_native_materialize

Generated from the hosted MCP registration. Do not edit this file directly.

[workflows router](../workflows.md) · [All categories](../index.md) · [Calling contract](../../mcp-calling.md)

Materialize a native TypeScript node with a stable instance id.

## When and how to call

- Effect: **write**.
- Scope: workspace plus selected project. Pass `projectId` explicitly for a multi-project connection; otherwise the connection default applies.
- Backend request: `POST /api/native-nodes/{catalogId}/materialize`. The MCP server fills the URL path, query, and body from the flat arguments below; do not send an HTTP request yourself.
- Result: the upstream response in `data`. Check MCP `isError` and JSON `ok`.

## Arguments

| Name | Requirement | Routed to | Type and constraints |
| --- | --- | --- | --- |
| `projectId` | optional | project scope | string; Project target; required for architecture-scoped sessions. |
| `catalogId` | required | URL path | string |
| `instanceId` | required | JSON body | string |
| `workflowId` | optional | JSON body | string |
| `position` | optional | JSON body | object |
| `params` | optional | JSON body | object |

Bundled input schema (use the live tool schema if the deployed server differs):

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    },
    "catalogId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "instanceId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "workflowId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "position": {
      "type": "object",
      "properties": {
        "x": {
          "type": "number"
        },
        "y": {
          "type": "number"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "additionalProperties": false
    },
    "params": {
      "type": "object",
      "maxProperties": 100
    }
  },
  "required": [
    "catalogId",
    "instanceId"
  ],
  "additionalProperties": false
}
```

## Example MCP tool call

```json
{
  "name": "project_native_materialize",
  "arguments": {
    "catalogId": "example-id",
    "instanceId": "example-id",
    "projectId": "example-project"
  }
}
```

Replace example IDs, paths, content, and revisions with live values. Never submit the zero revision.
