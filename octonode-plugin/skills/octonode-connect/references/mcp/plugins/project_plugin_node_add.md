# project_plugin_node_add

Generated from the hosted MCP registration. Do not edit this file directly.

[plugins router](../plugins.md) · [All categories](../index.md) · [Calling contract](../../mcp-calling.md)

Add one installed plugin node with a stable instance id.

## When and how to call

- Effect: **write**.
- Scope: workspace plus selected project. Pass `projectId` explicitly for a multi-project connection; otherwise the connection default applies.
- Backend request: `POST /api/plugins/{pluginId}/nodes/{nodeId}/add`. The MCP server fills the URL path, query, and body from the flat arguments below; do not send an HTTP request yourself.
- Result: the upstream response in `data`. Check MCP `isError` and JSON `ok`.
- Before calling: Inspect `project_plugin` and the target workflow graph before choosing a node and a stable instance ID. Read state before retrying an ambiguous add.

## Arguments

| Name | Requirement | Routed to | Type and constraints |
| --- | --- | --- | --- |
| `projectId` | optional | project scope | string; Project target; required for architecture-scoped sessions. |
| `pluginId` | required | URL path | string |
| `nodeId` | required | URL path | string |
| `instanceId` | required | JSON body | string |
| `workflowId` | optional | JSON body | string |
| `position` | optional | JSON body | object |

Bundled input schema (use the live tool schema if the deployed server differs):

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Project target; required for architecture-scoped sessions."
    },
    "pluginId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "nodeId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "instanceId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "workflowId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "position": {
      "type": "object",
      "properties": {
        "x": {
          "type": "number"
        },
        "y": {
          "type": "number"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "pluginId",
    "nodeId",
    "instanceId"
  ],
  "additionalProperties": false
}
```

## Example MCP tool call

```json
{
  "name": "project_plugin_node_add",
  "arguments": {
    "pluginId": "example-id",
    "nodeId": "example-id",
    "instanceId": "example-id",
    "projectId": "example-project"
  }
}
```

Replace example IDs, paths, content, and revisions with live values. Never submit the zero revision.
