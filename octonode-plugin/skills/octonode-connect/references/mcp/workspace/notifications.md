# notifications

Generated from the hosted MCP registration. Do not edit this file directly.

[workspace router](../workspace.md) · [All categories](../index.md) · [Calling contract](../../mcp-calling.md)

List notifications and unread count.

## When and how to call

- Effect: **read**.
- Scope: workspace. Pass any architecture or project identifier declared below.
- Backend request: `GET /api/social/notifications`. The MCP server fills the URL path, query, and body from the flat arguments below; do not send an HTTP request yourself.
- Result: the upstream response in `data`. Check MCP `isError` and JSON `ok`.

## Arguments

| Name | Requirement | Routed to | Type and constraints |
| --- | --- | --- | --- |
| `cursor` | optional | query | string |
| `limit` | optional | query | integer |

Bundled input schema (use the live tool schema if the deployed server differs):

```json
{
  "type": "object",
  "properties": {
    "cursor": {
      "type": "string"
    },
    "limit": {
      "type": "integer"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

## Example MCP tool call

```json
{
  "name": "notifications",
  "arguments": {}
}
```

Replace example IDs, paths, content, and revisions with live values. Never submit the zero revision.
