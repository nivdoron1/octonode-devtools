# profile_avatar_get

Generated from the hosted MCP registration. Do not edit this file directly.

[workspace router](../workspace.md) · [All categories](../index.md) · [Calling contract](../../mcp-calling.md)

Read a workspace member avatar as base64.

## When and how to call

- Effect: **read**.
- Scope: workspace. Pass any architecture or project identifier declared below.
- Backend request: `GET /api/profiles/{userId}/avatar`. The MCP server fills the URL path, query, and body from the flat arguments below; do not send an HTTP request yourself.
- Result: `data.contentType` and `data.base64`. Check MCP `isError` and JSON `ok`.

## Arguments

| Name | Requirement | Routed to | Type and constraints |
| --- | --- | --- | --- |
| `userId` | required | URL path | string |
| `v` | optional | query | integer |

Bundled input schema (use the live tool schema if the deployed server differs):

```json
{
  "type": "object",
  "properties": {
    "userId": {
      "type": "string"
    },
    "v": {
      "type": "integer"
    }
  },
  "required": [
    "userId"
  ],
  "additionalProperties": false
}
```

## Example MCP tool call

```json
{
  "name": "profile_avatar_get",
  "arguments": {
    "userId": "example-id"
  }
}
```

Replace example IDs, paths, content, and revisions with live values. Never submit the zero revision.
